
  # AI Brain Tumor Detection UI

  This is a code bundle for AI Brain Tumor Detection UI. The original project is available at https://www.figma.com/design/dyKfunXmS8leNBIoSIrcYm/AI-Brain-Tumor-Detection-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  